name: CD

on:
  push:
    branches:
      - main
      - dev

permissions:
  contents: read
  id-token: write

jobs:
  cd-web:
    name: Web CD
    uses: ./.github/workflows/web-cd.yml
    secrets: inherit
